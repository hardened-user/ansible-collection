# {{ ansible_managed }}

global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: "vmagent"
    static_configs:
      - targets: ["{{ vm_agent_docker_listen_addr }}:{{ vm_agent_docker_listen_port }}"]

scrape_config_files:
  - /etc/victoriametrics/vmagent/conf.d/*.yml
