---
- name: "Validate"
  ansible.builtin.include_tasks: "validate.yml"
  tags:
    - tinyproxy
    - validate


- name: "Create directories: ..."
  ansible.builtin.file:
    path: "{{ item.path }}"
    state: directory
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0755') }}"
  loop:
    - { path: "{{ tinyproxy_docker_compose_dir }}", mode: '0750' }
  when: item.enabled | default(true)
  tags:
    - tinyproxy
    - install


- name: "Copy file from template: {{ tinyproxy_docker_compose_dir }}/docker-compose.yml"
  ansible.builtin.template:
    src: "docker-compose.yml.j2"
    dest: "{{ tinyproxy_docker_compose_dir }}/docker-compose.yml"
    owner: root
    group: root
    mode: '0600'
  tags:
    - tinyproxy
    - install


- name: "docker-compose up: {{ tinyproxy_docker_compose_dir }}"
  community.docker.docker_compose_v2:
    project_src: "{{ tinyproxy_docker_compose_dir }}"
    pull: always
  tags:
    - tinyproxy
    - install
    - started
