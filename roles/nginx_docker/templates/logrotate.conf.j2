# {{ ansible_managed }}

{{ nginx_docker_logs_dir }}/*.log {
    {{ nginx_docker_logrotate_interval }}
    rotate {{ nginx_docker_logrotate_rotate }}
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
