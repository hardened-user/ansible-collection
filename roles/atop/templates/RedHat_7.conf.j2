# {{ ansible_managed }}

# Current Day format
CURDAY=`date +%Y%m%d`
# Log files path
LOGPATH={{ atop_log_dir }}
# Binaries path
BINPATH=/usr/bin
# PID File
PIDFILE=/var/run/atop.pid
# interval (default 10 minutes)
INTERVAL={{ atop_interval }}
