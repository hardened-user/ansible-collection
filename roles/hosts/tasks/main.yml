---
- name: "Validate"
  ansible.builtin.include_tasks: "validate.yml"
  tags:
    - hosts
    - validate


- name: "Install"
  ansible.builtin.include_tasks: "install.yml"
  tags:
    - hosts
    - install
